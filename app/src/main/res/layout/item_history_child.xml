<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="4dp"
    android:background="@color/flat_background"> <androidx.cardview.widget.CardView
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:cardBackgroundColor="@color/flat_card_bg"
    app:contentPadding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvHistoryName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="深蹲"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/flat_text_primary"/>

            <TextView
                android:id="@+id/tvHistoryDetails"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="100kg x 5组 x 5次"
                android:textSize="14sp"
                android:textColor="@color/flat_text_secondary"
                android:layout_marginTop="2dp"/>
        </LinearLayout>

        <ImageView
            android:id="@+id/btnEditHistory"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@android:drawable/ic_menu_edit"
            android:padding="6dp"
            android:layout_marginEnd="8dp"
            app:tint="@color/flat_text_secondary"/>

        <ImageView
            android:id="@+id/btnDeleteHistory"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@android:drawable/ic_menu_delete"
            android:padding="6dp"
            app:tint="#FF8A80"/> </LinearLayout>
</androidx.cardview.widget.CardView>
</LinearLayout>